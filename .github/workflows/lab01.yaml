name: lab01
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: pip install nbconvert
    - run: pip install pandoc
    - uses: actions/checkout@v4
    - run: python -m nbconvert --to pdf lab_01/pint_and_boiling_point.ipynb